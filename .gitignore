# Playwright MCP scratch output (screenshots, snapshots, console logs)
/.playwright-mcp/

# Local model weights — GGUFs are large (GBs); never commit them.
# Keep the directory in the tree (via .gitkeep) but ignore its contents.
/.models/*
!/.models/.gitkeep
